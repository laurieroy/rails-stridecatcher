<% flash.each do |type, message| %>
	<div class="alert alert-dismissible fade show <%= type == 'notice' ? 'alert-success' : 'alert-danger' %>" role="alert">
		<%= message %>
		<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
	</div>
	<% end %>